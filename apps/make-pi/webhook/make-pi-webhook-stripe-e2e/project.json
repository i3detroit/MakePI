{
  "name": "make-pi-webhook-make-pi-webhook-stripe-e2e",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "implicitDependencies": ["make-pi-webhook-stripe"],
  "targets": {
    "e2e": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{e2eProjectRoot}"],
      "options": {
        "jestConfig": "apps/make-pi/webhook/make-pi-webhook-stripe-e2e/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "apps/make-pi/webhook/make-pi-webhook-stripe-e2e/**/*.{js,ts}"
        ]
      }
    }
  }
}
